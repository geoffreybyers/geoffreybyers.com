---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---
<span
  aria-hidden="true"
  class={`inline-block transition-transform duration-200 ease-out group-hover:-translate-y-[2px] group-hover:translate-x-[2px] ${className}`}
>↗</span>
